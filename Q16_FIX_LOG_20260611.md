# L6.73.8 Q16 QA Closure Fix Log

## Scope

This package is rebuilt from the uploaded Q14 fix package and closes the Q16 findings plus one additional packaging defect found during clean extraction.

## Fixed items

1. ZIP entries now use POSIX `/` separators instead of backslashes, so Linux/macOS extraction preserves the directory tree.
2. `launchers/start_linyuanzhe_rc.py --help` now shows `FE01 STEP68 / L6.73.8`.
3. `backend/project/run_agent.py --mock --status` is read-only for package state and prints a non-absolute workspace label.
4. Historical release verifiers `verify_l660`, `verify_l661`, `verify_l662`, `verify_l663`, `verify_l664`, and `verify_l6708` are quick compatibility wrappers by default: no compileall, no package-tree report writes, and no cache generation.
5. `scripts/generate_launchers_l67220.py` fallback title now uses `FE01 STEP68 / L6.73.8`.
6. Heavy/host-dependent smoke entries now emit an immediate explicit `SKIP` unless their full-mode environment variable is set.
7. Desktop headless/Tk failure writes diagnostics outside the package tree and prints `<user-reports>/...` instead of an absolute package path.
8. POSIX launcher templates and regenerated launchers no longer print the absolute package root.
9. A false-positive secret-like token in the Round14 planning smoke sample text was renamed.

## Regression snapshot

- `start_linyuanzhe_rc.py --help`: PASS, no stale `FE01 STEP31 / L6.70`.
- `run_agent.py --mock --status`: PASS, no `.linyuanzhe`, no absolute package path.
- `verify_l660/l661/l662/l663/l664/l6708`: PASS within one second each, no package-tree mutations.
- `run_l67255`, `run_l6730`, and real-host smoke: explicit SKIP by default, exit 0, no package-tree mutations.
- `start_desktop_auto_l6719.sh` in headless mode: controlled rc=12, no package-tree reports, no absolute package path.
- Hygiene: no `.linyuanzhe`, no `reports` directory, no `__pycache__`, no `.pyc`, no BAT CRCRLF, no SH/COMMAND CRLF.


天工造物 L5 第二阶段开发日志

阶段名称：Manifest Schema、包描述、权限/资源/版本声明。
阶段边界：只做不可执行 Manifest Schema 与声明对象，不做真实插件加载、真实注册表写入、真实凭据解析、真实沙箱、真实外部动作或 L6 业务插件。

一、前置确认
- 以 L5 第一阶段交付包为输入基线。
- 第一阶段报告、validation、test_results、forbidden_scan、hash_compare、zip_integrity 文档存在。
- 开发前生成 L0-L4 hash baseline：docs/l5_phase2_pre_change_hash_manifest.txt。
- 开发前生成测试清单 baseline：docs/l5_phase2_pre_change_test_inventory.txt。

二、主要实现
- 新增 Manifest Schema / Validation / QualityGate / Hash 壳。
- 新增 Entry、MountSurface、Permission、Resource、Credential、DataGovernance、Audit、Version、Rollback、Compatibility、HotSwitch 声明对象。
- 新增 SourceTrustReference、SignatureReference 引用对象。
- 新增 CapabilityTokenDeclaration、TrustBoundaryDeclaration。
- 增强 PluginPackageDescriptor，使其可引用第二阶段声明对象。
- 保留 PluginManifestView 第一阶段字段白名单，不破坏第一阶段冻结测试；第二阶段质量门可读取内存 Manifest-like payload 与声明对象。

三、验证摘要
- compileall exit_code=0
- collect-only exit_code=0, 1006 tests collected in 4.78s
- targeted pytest exit_code=0, 43 passed in 5.07s
- plugin_host subset exit_code=0, 46 passed, 960 deselected in 5.66s
- full pytest exit_code=0, 1006 passed in 13.71s
- L0-L4 hash compare：0 added / 0 modified / 0 deleted
- forbidden scan：0 blocking findings

四、阶段结论
P0=0，P1=0，P2=0。
建议进入 L5 第三阶段。

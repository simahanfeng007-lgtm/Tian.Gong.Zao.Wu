L5 第八阶段 hotfix1 质检修复报告

输入依据：
桌面 L5 质检报告文件夹中的 P1/P2/P3 问题。

P1 修复：
1. L5FinalQualityGateDecision 改为 fail-closed 硬门派生。
2. no_live_artifact_build_passed、public_projection_second_leak_test_passed、capability_readiness_matrix_passed、generic_plugin_host_precheck_passed、blocking_reasons、P2/P3、Context/Belief/World 边界全部纳入 freeze hard gate。
3. PublicProjection 安全检查改为 key/value 同扫，拒绝危险字段名、真实路径和 secret-like pattern。
4. PublicProjectionBuilder 只输出 redacted evidence refs。
5. L5L6HandoffFreeze / L5FinalAuditIndex 强制责任链、审计链、MessageEnvelope-first 关键 refs。

P2 修复：
1. L5CapabilityReadinessMatrix 拒绝空矩阵、缺行、非法状态、blocking reasons 与 Product Artifact Factory 执行误读。
2. GovernanceCoverageMatrix 校验 coverage row 状态。
3. L5/L6 HandoffFreeze 增补 Context/Belief/World、SkillToolRelease、SelfHealing、SelfEvolution、MemoryForgetting、ResourceBudget、VersionMigration refs。
4. L3 Run Loop 增补 SkillToolReleaseFlow、ExecutionHandoffFlow、ObservationFeedbackFlow。
5. 新增 L1/L2 communication handoff 直接测试。

P3 修复：
1. pytest.ini 增加 pythonpath = .。
2. 重打包时使用 phase8 语义根目录。
3. 更新最终冻结文档与测试证据。

验证结论：
compileall passed。
collect-only 1308 tests collected。
L5 phase8 targeted 69 passed。
full pytest 1308 passed。
pytest -q 1308 passed。
forbidden scan blocking_findings=0。

最终结论：
L5 第八阶段 hotfix1 后 P0=0、P1=0，建议作为 L5 最终冻结包。
允许进入 L6 一般插件规划。
Product Artifact Factory 仅允许进入 L6 专项规划，不允许直接进入真实执行实现。

L5 Governance Coverage Matrix（hotfix2 情志专项补丁后）

插件形态：
ToolPlugin / SkillPlugin / MemoryPlugin / PolicyPlugin / AdapterPlugin / GovernancePlugin / ObservationPlugin / CommunicationHandoffPlugin / SelfHealingPlugin / SelfEvolutionPlugin / MemoryGovernancePlugin / ForgettingPlugin / ProductionPlugin / ArtifactFactoryPlugin / ProductBuilderPlugin / DeliveryPlugin / ValidationPlugin / AffectivePlugin。

治理门：
Event / Effect / Lease / Policy / Contract / RiskDecision / HumanGate / AuditEvidence / ResourceBudget / PrivacySecret / VersionMigration / TestValidationRegression / TransactionCompensation / ArtifactProvenanceIntegrity / ContextSafetyBoundary / BeliefEventPrecedence / WorldStateEvidenceStaleness / ToolOutputDemotion / ModelOutputDemotion / MemoryInjectionBoundary / ContextPollutionReview / MessageEnvelopeFirst / ConversationChannelProtocol / AckNackResultReturn / SelfHealingRecoveryPlan / SelfEvolutionCommitBoundary / MemoryForgettingGovernance / SkillToolReleaseBoundary。

情志专项覆盖：
- AffectivePlugin 已作为通用插件宿主声明行纳入 coverage_rows，不是 ToolPlugin 伪装。
- 情志插件必须经过 Event、Effect、Lease、Policy、Contract、RiskDecision、HumanGate、AuditEvidence、ResourceBudget、PrivacySecret、VersionMigration、TestValidationRegression、TransactionCompensation、ContextSafetyBoundary、MemoryInjectionBoundary 等治理门。
- 情志调制只允许进入 L6 规划引用，不能成为执行令、授权、预算豁免、风险裁决覆盖依据或记忆写删晋升依据。

校验规则：
- governance_gate_refs 必须覆盖所有 core gates。
- coverage_rows 必须覆盖每一个 plugin_kind_ref。
- 每一行状态必须为 covered。
- missing / partial / unknown / blocked 均不得通过最终冻结。

结论：
全部覆盖。ProductionPlugin / ArtifactFactoryPlugin / AffectivePlugin 均仅为声明级 L6 规划前提，不具备执行权限。

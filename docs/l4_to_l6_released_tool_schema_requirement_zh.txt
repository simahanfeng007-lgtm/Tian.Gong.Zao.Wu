天工造物 L4→L6 已释放工具 Schema 专项需求附录

日期：2026-06-04

一、L6 后续接管范围

1. 真实工具 schema 生成。
2. 真实工具适配器。
3. 真实模型适配器。
4. 真实工具执行来源。
5. 真实结果封装来源。
6. ToolRegistry / SkillRegistry / PluginHost 只能在 L6/L5 设计许可范围内实现，不能由 L4 实现。

二、L4 当前职责

L4 仅提供 ReleasedToolSchemaView、ModelVisibleReleasedToolView、SkillToolReleaseSessionContext、ToolResultReturnContext 和 SkillToolReleaseChainIndex 作为只读引用形状。

三、禁止

L4 不得创建真实工具 schema，不得持有真实工具句柄，不得调用工具，不得调用模型，不得实现插件宿主。

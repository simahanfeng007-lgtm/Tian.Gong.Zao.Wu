# CHANGELOG L6.70.5 / FE01 STEP31E

- 修复桌面端在缺少 Tcl/Tk 的 Python 环境下直接闪退的问题，新增 Tk 预检与失败报告。
- 新增自动 Python 选择启动器，优先使用 `py -3.12/3.11/3.10`，避免误用缺少 Tk 的 Python315/minimal/embeddable。
- 修复启动脚本工作目录漂移，统一 `cd /d "%~dp0"`。
- 写入授权改为选择输出目录，不再使用类似上传文件的写入授权交互。
- MCP 注册提交增加可见回执与状态栏提示。
- 记忆页改为紧凑布局，减少空白。
- 侧边栏新增三套配色快捷按钮：极夜、暖灰、墨绿。

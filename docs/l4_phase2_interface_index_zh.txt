天工造物 L4 第二阶段接口清单
生成日期：2026-06-04

一、许可引用
PermitScope、PermitExpiry、ActionPermitRef、ExecutionPermitRef、PermitIssuerRef、PermitSubjectRef、PermitActionRef、PermitEnvironmentRef、PermitConsumptionRef。

二、边界与 L5 引用
BoundaryDecisionRef、PolicyDecisionRef、PermissionGrantRef、RiskReviewRef、ConfirmationTicketRef、LeaseRef、CredentialHandleRef、AuditRequirementRef、ResourceLimitRef。

三、门控输入与结果
ActionGroundingGateInput、ExecutionGateInput、ActionGroundingGateResult、ExecutionGateResult、ActionGroundingPermitRequirement、ActionGroundingBoundaryRequirement。

四、校验结果
PermitValidationStatus、PermitValidationReason、PermitValidationTrace、PermitValidationResult。

五、校验器
ActionGroundingGateValidator、ExecutionGateValidator。
说明：只做结构校验，不做政策裁决或风险裁决。

六、失败对象
PermitMissingFailure、PermitDeniedFailure、PermitExpiredFailure、PermitScopeMismatchFailure、PermitMalformedFailure、PermitTestOnlyMisuseFailure、BoundaryMissingFailure、BoundaryDeniedFailure、BoundaryExpiredFailure、BoundaryScopeMismatchFailure、CredentialUnavailableFailure、CredentialScopeMismatchFailure、ResourceLimitUnavailableFailure、ResourceLimitExceededFailure、AuditRequirementMissingFailure、LeaseUnavailableFailure。

七、L5 port 占位
L5PermitValidatorPort、L5CredentialResolverPort、L5AuditSinkPort、L5ResourceBudgetPort、L5BoundaryRecheckPort、L5PermitConsumptionReporterPort、L5BoundaryFeedbackPort。
说明：均为 Protocol 占位，不提供真实实现。

八、test-only 与不变量
FakeBoundaryPermitForTestOnly、FakePermitIssuerForTestOnly、SyntheticBoundaryDecisionForTestOnly、NoLiveActionWithoutL5PermitInvariant、TestOnlyPermitNeverProductionInvariant、NoL4PermissionDecisionInvariant、NoL4RiskDecisionInvariant、NoL4TicketIssuerInvariant、NoL4CredentialResolverInvariant、NoL4AuditWriterInvariant。

# 五大模型专项 hotfix3 未做事项

1. MiMo 普通 API 的精确 endpoint、错误码、streaming 细节仍需在官方 API 文档可解析后补齐；当前只保留 ref-only 与 unknown。
2. MiMo Token Plan 各地区 endpoint 不写死，后续只能通过 L5 credential/provider scope 注入 ref。
3. L4 live adapter skeleton 仍保持 disabled-by-default，未启用真实 API 调用。
4. L6 真实模型插件仍不建议直接开发；可以进入 L6 规划/一般插件开发。
5. 后续若正式启用 provider live adapter，必须另开 L5 permit 签发与 L4 live 适配专项，并保留 A5/预算/审计/凭据链。

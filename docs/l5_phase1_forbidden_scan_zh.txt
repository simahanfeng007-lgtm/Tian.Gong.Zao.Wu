天工造物 L5 第一阶段 forbidden scan 报告
生成日期：2026-06-04

一、扫描范围
- tiangong_kernel/l5_plugin_host/**/*.py
- tests/test_l5_phase1_*.py
- docs/l5_phase1_*.txt

二、扫描类别
- 动态代码载入与解释执行类高风险入口。
- 子进程、网络客户端、系统命令、危险反序列化、底层扩展库、并发任务启动类入口。
- 真实文件读写、删除、注册表写入、沙箱创建、凭据读取类入口。
- 旧能力包链、旧主链与旧中文主控名回潮。
- 第一阶段禁止的真实生命周期调度语义与修改型注册表方法。

三、结果
- 阻断命中：0。
- L5 源码阻断命中：0。
- L5 测试阻断命中：0。
- L5 文档阻断命中：0。

四、说明
- L5 第一阶段代码没有动态载入插件、没有调用 L4 真实动作实现、没有创建沙箱、没有读取凭据、没有写真实注册表。
- RegistrySnapshot 只保留不可变快照语义。
- HandoffIndex 只接收调用方传入的 ref/title/summary/digest/path_hint/source_layer 元数据，不读取真实文件。
- 质量门对象只记录验证结果，不执行验证命令。

五、结论
forbidden scan passed。

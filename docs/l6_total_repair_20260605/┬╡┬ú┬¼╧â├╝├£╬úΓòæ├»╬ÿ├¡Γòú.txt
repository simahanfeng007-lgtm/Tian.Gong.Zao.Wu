# L6 总修复未做事项

1. 未实现真实 L3 消费适配；当前只做 ref-only 对象补强。
2. 未实现真实成品生产、真实自愈、真实自学习/自进化执行；这些必须后续阶段单独设计。
3. 未将情感系统 governance_reason 默认值强制改为无默认；本次只保留现有安全约束并补 scan/safety 检查。
4. 未大规模重构插件准入 `approved_declared` 默认值，避免破坏现有公共契约；已通过契约补丁质量门加强冻结控制。
5. 未修改 L0-L5。若后续要真实接入执行链，必须另做跨层适配和回归。
天工造物 L6.9 外壳式最小智能体启动器修复报告
生成日期：2026-06-05

一、结论

已完成一版“不污染主体”的 L6.9 最小启动器。

本轮没有把 CLI、API 配置、模型调用、session、工具桥写入 tiangong_kernel 主体；而是在 engine 根目录新增 run_agent.py，并新增独立外壳包 tiangong_agent_shell/。

当前可用能力：
- python run_agent.py --help
- python run_agent.py --mock --once "你好"
- python run_agent.py --mock 进入交互模式
- python -m tiangong_agent_shell --mock --once "你好"
- python run_agent.py --status
- OpenAI-compatible 非流式最小调用代码路径
- API Key 状态/配置输出脱敏
- 工具桥默认 disabled，不执行真实工具

二、架构口径

采用外壳式启动器：

L6 主体：engine/tiangong_kernel/
- 保持冻结；
- 不新增 CLI；
- 不新增 API Client；
- 不保存 API Key；
- 不反向 import 外壳层。

L6.9 外壳：engine/tiangong_agent_shell/
- 负责 CLI；
- 负责配置加载；
- 负责 session；
- 负责模型客户端适配；
- 负责 mock 模型；
- 负责工具桥占位；
- 负责安全脱敏。

三、主体污染检查

Hash Compare 结果：
- 范围：engine/tiangong_kernel/**
- 原始内核文件数：725
- 当前内核文件数：725
- 新增内核文件：0
- 删除内核文件：0
- 变更内核文件：0
- 结论：PASS，tiangong_kernel 既有文件未变更。

四、验证结果

已执行：
- python run_agent.py --help：通过
- python run_agent.py --mock --once "你好"：通过
- python -m tiangong_agent_shell --mock --once "你好"：通过
- python run_agent.py --status：通过
- python run_agent.py --show-config --api-key sk-1234567890abcdef ...：脱敏通过
- python -m compileall tiangong_kernel tiangong_agent_shell run_agent.py：通过
- pytest -q：1720 passed in 21.78s

五、真实 API 状态

OpenAI-compatible 最小非流式调用代码已实现，但本环境没有用户真实 API Key，也没有对外部 Provider 发起真实请求。
因此当前结论应写为：
- 代码路径已具备最小调用能力；
- 未做真实 API 联调；
- 不能宣称全部 Provider 可用；
- 后续 L7 需做真实 DeepSeek / OpenAI-compatible / Qwen / GLM / MiniMax / MiMo 等 Provider 联调。

六、未做内容

- 未做桌面前端；
- 未做 Windows/macOS/Linux 安装包；
- 未做 python -m tiangong_kernel 转发入口，原因是本轮优先防止污染主体；
- 未开放真实工具执行；
- 未接长期记忆写入；
- 未做真实外部 API 联调。

七、建议下一步

先让工程师在本地用 mock 验收：
python run_agent.py --mock --once "你好"

再用一个真实 OpenAI-compatible endpoint 验收：
python run_agent.py --provider openai_compatible --base-url <url> --api-key <key> --model <model> --once "你好"

真实 API 联调通过后，再进入 L7 前端和安装包。

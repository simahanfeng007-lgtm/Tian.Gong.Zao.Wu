天工造物 数学模型发动机 L1-L4 100 分前置基础开发日志
日期：2026-06-04

本补丁基于数学模型发动机 L1-L4 前置基础方案包，补齐 L1 契约端口、L2 治理状态、L3 Flow、L4 禁用态适配器骨架与专项测试。

完成内容：
1. L1 新增 math_engine_contract_ports.py，覆盖模型定义、特征、评分、证据、校准、漂移、遥测、治理、回放、影子和冲突端口。
2. L2 新增 math_model_governance_state.py，覆盖注册、参数、特征、评分、证据、校准、漂移、回放、影子、治理、冲突和遥测状态。
3. L3 新增 math_model_engine_flow.py，覆盖特征提取、评分、聚合、置信合成、冲突、校准、漂移、回放、影子、降级和审计投影 Flow。
4. L4 新增 math_model_adapter.py，覆盖 Base、DeterministicLocalScore、ExternalModel、Replay、Shadow、Fallback 适配器骨架。
5. 新增 13 个专项测试文件，锁定 L1-L4 只引用、不执行、不裁决、默认禁用和公式参数外置。

边界声明：
1. 不实现真实模型。
2. 不调用网络、文件、终端、工具、外部模型或第三方数学库。
3. 不把模型评分变成权限、动作或最终裁决。
4. 不写 L2 状态，不写审计库。
5. 公式、权重、阈值只以外置引用表达。

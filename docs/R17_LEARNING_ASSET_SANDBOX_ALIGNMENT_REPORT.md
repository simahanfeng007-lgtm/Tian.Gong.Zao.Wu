# L6.70.2-R17 ToolSkill 统一资产契约与既有沙箱对齐报告

沙箱已找到：L6.22 Tool 生产请求沙箱化与验证前置链。R17 将 R16 统一资产契约对齐到该链，不新建真实执行沙箱。

新增：`learning_asset_sandbox_guide / align / validate`，命令：`asset-sandbox guide / align / validate / drill`。

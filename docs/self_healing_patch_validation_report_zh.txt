天工造物 自愈系统最小补丁验证报告
日期：2026-06-04

验证结论：通过专项自检。

验证范围：
1. L1 诊断、恢复计划、生命周期、恢复后验证、复盘端口均只定义协议。
2. L2 状态均为 frozen + slots 的 ref-only 状态。
3. L3 Flow / advice 均 advisory_only，不执行恢复、回滚、写状态或签票。
4. L3 ready 状态必须具备 checkpoint、validation、regression 引用。
5. L4 handoff 不执行恢复、不执行回滚、不写 L2、不写审计。

已执行：
1. python -m compileall -q tiangong_kernel tests
2. python -m pytest -q tests/test_l1_self_healing_ports_are_protocol_only.py ... tests/test_self_healing_chain_refs_only.py

结果：
1. compileall 通过。
2. 专项测试 6 passed。

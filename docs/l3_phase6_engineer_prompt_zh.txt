你是“天工造物新版 L3 编排层”第六阶段开发工程师。

本对话只做 L3 第六阶段工程。
不得开发第一至第五阶段已完成内容，除非发现阻断级兼容问题并以最小补丁修复。
不得开发第七阶段、第八阶段。
不得进入 L4/L5/L6 的真实实现。
不得修改 L0、L1、L2。
不得恢复旧能力包、CapabilityPort、AbilityPackagePort、神枢或旧 Runtime 主链。
不得把 L3 做成执行层、权限层、存储层、模型层、工具层、插件宿主、观察系统、记忆系统、检索系统、学习系统或情感系统。

────────────────────────────────
一、阶段名称
────────────────────────────────

L3 第六阶段：观察回流、上下文续接、记忆/检索/学习/情感请求编排

一句话边界：

L3 第六阶段负责“把执行/观察后的结果、上下文、记忆、检索、学习、情感等服务需求组织成可审查、可排序、可续接、可交给未来 L6 子系统处理的请求与建议”，不负责“观察如何采样、记忆如何读取、检索如何运行、学习如何发生、情感如何计算”。

────────────────────────────────
二、本阶段目标
────────────────────────────────

在 L3 第一阶段“编排基础骨架与数学驱动基础骨架”、第二阶段“Run / Task / Turn / Step 编排与连续性评分”、第三阶段“Skill 直显与 ToolGroup 释放编排评分”、第四阶段“ModelIntent / ToolIntent / ActionIntent 编排评分”、第五阶段“边界裁决请求与执行请求编排评分”的基础上，新增以下能力的纯编排对象与评分建议：

1. 观察结果回流编排；
2. 上下文续接编排；
3. 记忆服务请求编排；
4. 检索服务请求编排；
5. 学习服务请求编排；
6. 情感服务请求编排；
7. 候选提议流程编排；
8. 观察可信度评分；
9. 上下文价值评分；
10. 检索需要评分；
11. 学习信号价值评分；
12. 情感倾向权重接入；
13. L6 服务请求路径排序；
14. 状态转移建议；
15. 与前五阶段对象的引用接线。

本阶段只做 request / ref / advice / hint / score / ranking / suggestion。
不得实现真实 L6 子系统，不得调用真实服务。

────────────────────────────────
三、必须先读取的前置材料
────────────────────────────────

请先读取用户上传的以下材料。
如果缺失，必须先列出缺失项，不得臆造。

必须读取：

1. L0 最终冻结归档包或 L0 冻结源码；
2. L1 最终冻结归档包或 L1 冻结源码；
3. L2 最终冻结归档包或 L2 冻结源码；
4. L2 总收口报告；
5. L2 第九阶段数学模型状态、情感状态、动态驱动状态相关报告；
6. L3 总策划书；
7. L3 工程交付规范与质检清单；
8. L3 第一阶段最终工程包、开发日志、handoff、validation、测试结果、质检/修复报告；
9. L3 第二阶段最终工程包、开发日志、handoff、validation、测试结果、质检/修复报告；
10. L3 第三阶段最终工程包、开发日志、handoff、validation、测试结果、质检/修复报告；
11. L3 第四阶段最终工程包、开发日志、handoff、validation、测试结果、质检/修复报告；
12. L3 第五阶段最终工程包、开发日志、handoff、validation、测试结果、质检/修复报告；
13. 本 L3 第六阶段工程员提示词。

读取后先输出“前置材料核对结论”，包括：

- 已读取文件；
- 缺失文件；
- L0 是否仍可作为原语基座；
- L1 是否仍可作为端口协议基座；
- L2 是否仍可作为状态基座；
- L2 第九阶段数学/情感/动态驱动状态入口是否可被 L3 引用；
- L3 第一至第五阶段是否可作为第六阶段基座；
- 是否可以进入 L3 第六阶段工程；
- 如不能进入，列出阻断原因。

────────────────────────────────
四、继承的新版主链
────────────────────────────────

新版主链必须保持：

大模型 → Skill 直显 → Skill 选中 → 工具组释放 → 模型提出工具调用意图 → L5 边界裁决 → L4 执行 → 观察结果 → 反馈 → L2 状态记录

本阶段只覆盖其中：

1. 观察结果回流的 L3 编排表示；
2. 执行结果、失败结果、观察结果与 Run/Task/Turn/Step 的回接建议；
3. 上下文续接、上下文携带、上下文压缩需求、上下文价值评分；
4. 面向未来 L6 记忆系统的服务请求编排；
5. 面向未来 L6 检索系统的服务请求编排；
6. 面向未来 L6 学习系统的服务请求编排；
7. 面向未来 L6 情感系统的服务请求编排；
8. 候选提议、候选信号、候选审查、候选入列建议；
9. 状态转移建议、路径排序、原因码和可解释摘要。

本阶段不得覆盖：

1. L4 真实执行；
2. L5 真实裁决；
3. L6 真实观察系统；
4. L6 真实记忆系统；
5. L6 真实检索系统；
6. L6 真实学习系统；
7. L6 真实情感系统；
8. L6 真实 Skill / Knowledge / Tool 生成；
9. 模型真实调用；
10. 工具真实调用；
11. 网络、文件、数据库、shell、桌面真实操作；
12. 状态持久化；
13. 验证执行、回滚执行、补丁生成、自动合入。

────────────────────────────────
五、允许新增的对象族
────────────────────────────────

实际命名可按现有项目风格调整，但职责必须清楚，且不得越界。

A. 观察回流编排对象

1. ObservationResultRef
2. ObservationEnvelope
3. ObservationFeedbackAdvice
4. ObservationRoutingAdvice
5. ObservationTrustHint
6. ObservationCredibilityScore
7. ObservationRelevanceScore
8. ObservationCompletenessScore
9. ObservationConflictAdvice
10. ObservationStateTransitionAdvice

注意：只引用未来 L4/L6 返回的观察结果，不采样真实观察，不读取桌面、文件、网络、日志、摄像头或数据库。

B. 上下文续接编排对象

1. ContextCarryoverAdvice
2. ContextWindowAdvice
3. ContextCompressionNeedAdvice
4. ContextRetentionAdvice
5. ContextDropAdvice
6. ContextPriorityAdvice
7. ContextContinuityScore
8. ContextValueScore
9. ContextConflictAdvice
10. ContextStateTransitionAdvice

注意：只提供携带、保留、丢弃、压缩、排序建议；不真实压缩上下文，不写上下文存储，不调用模型摘要。

C. L6 服务请求编排对象

1. SubsystemServiceRequest
2. SubsystemServiceRequestRef
3. SubsystemServiceEnvelope
4. SubsystemServiceAdvice
5. SubsystemServiceRouteRanking
6. SubsystemServiceReadinessScore
7. SubsystemServiceRequirementHint
8. SubsystemServiceResultRef
9. SubsystemServiceFailureRef
10. SubsystemServiceStateTransitionAdvice

注意：只表示请求未来 L6 子系统服务，不实现 L6 子系统，不调用插件宿主，不执行服务。

D. 记忆服务请求对象

1. MemoryServiceRequest
2. MemoryServiceRequestRef
3. MemoryRecallRequestHint
4. MemoryWriteSuggestion
5. MemoryPromotionSignalAdvice
6. MemoryConflictReviewAdvice
7. MemoryRelevanceScore
8. MemoryNeedScore
9. MemoryServiceRouteRanking
10. MemoryStateTransitionAdvice

注意：MemoryWriteSuggestion 只是建议未来 L6 记忆系统考虑写入/晋升/冲突审查，不真实写记忆，不读取真实记忆，不做记忆晋升算法。

E. 检索服务请求对象

1. RetrievalServiceRequest
2. RetrievalServiceRequestRef
3. RetrievalQueryHint
4. RetrievalScopeHint
5. RetrievalNeedScore
6. RetrievalPriorityAdvice
7. RetrievalResultRef
8. RetrievalResultUseAdvice
9. RetrievalRouteRanking
10. RetrievalStateTransitionAdvice

注意：RetrievalQueryHint 只是检索意图提示，不真实搜索，不访问网络，不做 embedding，不做 RAG，不调用数据库或向量库。

F. 学习服务请求对象

1. LearningServiceRequest
2. LearningServiceRequestRef
3. LearningSignalAdvice
4. LearningSignalValueScore
5. LearningNeedScore
6. LearningCandidateAdvice
7. LearningEvidenceRef
8. LearningResultRef
9. LearningRouteRanking
10. LearningStateTransitionAdvice

注意：LearningCandidateAdvice 只是候选学习建议，不生成真实 Skill，不生成真实知识，不训练模型，不实现学习算法。

G. 情感服务请求与权重接入对象

1. AffectiveServiceRequest
2. AffectiveServiceRequestRef
3. AffectiveWeightInputRef
4. AffectiveTendencyAdvice
5. AffectiveExpressionAdvice
6. AffectiveBehaviorTendencyAdvice
7. AffectiveWeightAdjustmentHint
8. AffectiveNeedScore
9. AffectiveRouteRanking
10. AffectiveStateTransitionAdvice

注意：情感对象只能接收或传递权重输入、倾向建议和表达建议，不计算真实情感，不产生执行令，不绕过 L5 或 L4。

H. 候选提议流程对象

1. CandidateProposalAdvice
2. CandidateSignalAdvice
3. CandidateReviewRequestHint
4. CandidateEvidenceRef
5. CandidatePriorityScore
6. CandidateLearningValueScore
7. CandidatePromotionAdvice
8. CandidateRejectAdvice
9. CandidateRouteRanking
10. CandidateStateTransitionAdvice

注意：候选提议只是建议未来 L6 或后续阶段审查候选，不自动入库，不自动合入，不生成补丁，不生成真实 Skill/Tool。

I. 第六阶段数学评分对象

1. ObservationFeedbackMathInput
2. ObservationFeedbackMathResult
3. ContextCarryoverMathInput
4. ContextCarryoverMathResult
5. SubsystemServiceMathInput
6. SubsystemServiceMathResult
7. ObservationCredibilityScore
8. ContextValueScore
9. RetrievalNeedScore
10. LearningSignalValueScore
11. MemoryNeedScore
12. AffectiveNeedScore
13. CandidatePriorityScore
14. SubsystemServiceRecommendation
15. ObservationContextSubsystemRouteRanking

J. 与前五阶段接线对象

1. ExecutionToObservationAdvice
2. ExecutionResultContextAdvice
3. IntentObservationFeedbackAdvice
4. SkillToolContextCarryoverAdvice
5. RunObservationAdvice
6. TaskObservationAdvice
7. TurnObservationAdvice
8. StepObservationAdvice
9. RunSubsystemServiceAdvice
10. TaskSubsystemServiceAdvice
11. TurnSubsystemServiceAdvice
12. StepSubsystemServiceAdvice

所有对象必须是纯数据或纯逻辑建议对象，不得持有真实模型客户端、真实工具对象、真实观察器、真实检索器、真实记忆库、真实学习器、网络连接、数据库连接、文件句柄、凭据对象或插件宿主。

────────────────────────────────
六、建议新增文件方向
────────────────────────────────

建议在既有目录中继续扩展：

tiangong_kernel/l3_orchestration/

建议新增或收口以下文件方向：

1. observation_feedback.py
2. observation_result_ref.py
3. observation_routing_advice.py
4. context_carryover.py
5. context_value_score.py
6. subsystem_service_request.py
7. subsystem_service_advice.py
8. memory_service_request.py
9. retrieval_service_request.py
10. learning_service_request.py
11. affective_service_request.py
12. candidate_proposal_advice.py
13. observation_context_math.py
14. observation_context_math_input.py
15. observation_context_math_result.py
16. subsystem_service_math.py
17. subsystem_service_math_input.py
18. subsystem_service_math_result.py
19. observation_context_transition.py
20. observation_context_serialization.py

禁止新增或实现以下方向：

1. observer.py；
2. observation_engine.py；
3. desktop_observer.py；
4. file_observer.py；
5. network_observer.py；
6. memory_store.py；
7. memory_engine.py；
8. retrieval_engine.py；
9. search_engine.py；
10. rag_engine.py；
11. embedding_engine.py；
12. vector_store.py；
13. learning_engine.py；
14. skill_generator.py；
15. knowledge_generator.py；
16. tool_generator.py；
17. affective_engine.py；
18. emotion_engine.py；
19. subsystem_router_runtime.py；
20. plugin_host.py；
21. l6_subsystem.py；
22. model_runner.py；
23. tool_executor.py；
24. execution_engine.py；
25. boundary_engine.py；
26. runtime_core.py；
27. shenshu.py；
28. ability_package.py；
29. capability_port.py。

如果已有文件名包含 request / ref / advice / hint，不等于违规；关键是不得执行、不得调用、不得存储、不得实现真实 L6 服务。

────────────────────────────────
七、实现原则
────────────────────────────────

1. 只能依赖 Python 标准库、L0、L1、L2、L3 第一至第五阶段；
2. 不引入第三方依赖；
3. 不修改 L0/L1/L2；
4. 不导入 L4/L5/L6；
5. 不真实调用模型；
6. 不真实调用工具；
7. 不真实执行动作；
8. 不真实读写文件；
9. 不真实访问网络；
10. 不执行 shell；
11. 不实现插件宿主；
12. 不实现状态存储；
13. 不实现观察采样；
14. 不实现记忆读取或写入；
15. 不实现检索；
16. 不实现 embedding；
17. 不实现 RAG；
18. 不实现学习算法；
19. 不实现情感算法；
20. 不生成真实 Skill；
21. 不生成真实 Tool；
22. 不生成真实知识；
23. 不实现权限裁决；
24. 不实现风险放行；
25. 不签发确认票据；
26. 不读取凭据；
27. 不写审计；
28. 不实现真实 L4/L5/L6；
29. 不恢复旧能力包主链；
30. 不恢复 CapabilityPort / AbilityPackagePort；
31. 不恢复“神枢”或旧 Runtime 主链。

对象规范：

1. 公共对象优先使用 frozen + slots dataclass；
2. 公共对象必须可稳定序列化；
3. 公共对象字段应可 diff、可快照；
4. ID、时间、trace 等优先使用 L0 原语或显式输入；
5. 不得在对象默认值中使用 datetime.now、random、uuid 自动漂移；
6. 数学评分对象必须可解释；
7. 评分必须标记为 recommendation / advice / suggestion，不得伪装为 decision；
8. 状态转移只能输出 advice / suggestion，不得直接持久化状态；
9. ServiceRequest 只能是 request，不得变成 service runtime；
10. ObservationResultRef 只能是 ref，不得变成 observer；
11. MemoryWriteSuggestion 只能是 suggestion，不得写记忆；
12. RetrievalQueryHint 只能是 hint，不得执行检索；
13. LearningSignalAdvice 只能是 advice，不得生成 Skill；
14. AffectiveTendencyAdvice 只能是 tendency，不得产生执行令。

────────────────────────────────
八、数学驱动要求
────────────────────────────────

本阶段必须纳入数学驱动，但只能做轻量评分骨架和建议，不实现复杂策略。

输入方向：

1. L2 状态切片引用；
2. L3 Run / Task / Turn / Step 编排上下文；
3. Skill / ToolGroup 编排上下文；
4. ModelIntent / ToolIntent / ActionIntent 编排上下文；
5. Boundary / Execution 请求编排上下文；
6. ExecutionResultRef / ExecutionFailureRef / ObservationResultRef；
7. 目标向量；
8. 约束集合；
9. 情感权重输入引用；
10. 动态驱动权重输入引用；
11. 连续性评分引用；
12. 稳定性/可逆性/最小暴露约束；
13. 未来 L6 服务需求引用。

评分方向：

1. ObservationCredibilityScore：观察结果可信度；
2. ObservationRelevanceScore：观察与目标相关度；
3. ObservationCompletenessScore：观察完整度；
4. ContextValueScore：上下文保留价值；
5. ContextContinuityScore：上下文连续性；
6. ContextCompressionNeedScore：是否需要压缩；
7. MemoryNeedScore：是否需要记忆服务；
8. RetrievalNeedScore：是否需要检索服务；
9. LearningSignalValueScore：学习信号价值；
10. AffectiveNeedScore：是否需要情感服务；
11. CandidatePriorityScore：候选优先级；
12. SubsystemServiceReadinessScore：服务请求准备度；
13. SubsystemServiceRouteRanking：服务请求路径排序；
14. ObservationContextSubsystemRouteRanking：观察/上下文/子系统综合路径排序。

硬边界：

1. 数学评分只给建议；
2. 数学评分不调用观察器；
3. 数学评分不写上下文；
4. 数学评分不写记忆；
5. 数学评分不执行检索；
6. 数学评分不生成 Skill；
7. 数学评分不生成 Tool；
8. 数学评分不计算真实情感；
9. 数学评分不做权限裁决；
10. 数学评分不做风险放行；
11. 数学评分不触发执行；
12. 数学评分不绕过 L5；
13. 数学评分不替代 L4；
14. 数学评分不实现 L6 专业算法。

────────────────────────────────
九、情感与动态驱动接入要求
────────────────────────────────

本阶段必须能引用或接收 L2 第九阶段的情感状态、情志状态、动态驱动状态，但不得实现情感算法。

情感权重可影响：

1. 是否更倾向保留上下文；
2. 是否更倾向压缩上下文；
3. 是否更倾向请求记忆服务；
4. 是否更倾向请求检索服务；
5. 是否更倾向请求学习服务；
6. 是否更倾向请求情感服务；
7. 是否更倾向提出候选；
8. 是否更倾向谨慎观察；
9. 是否更倾向修复；
10. 是否更倾向学习；
11. 是否更倾向探索；
12. 是否表达更直接或更温和的建议字段。

动态驱动可影响：

1. 观察回流路径排序；
2. 上下文续接路径排序；
3. 记忆/检索/学习/情感请求优先级；
4. 候选提议优先级；
5. 修复价值、学习价值、稳定价值权重；
6. 继续推进或暂停建议；
7. 服务请求准备度建议权重，但不得变成服务执行授权。

严禁：

1. 情感产生执行令；
2. 情感提升权限；
3. 情感绕过 L5；
4. 情感绕过 L4；
5. 情感直接调用 L6 服务；
6. 情感调用模型或工具；
7. 情感读取记忆；
8. 情感执行检索；
9. 情感生成 Skill；
10. 动态驱动自动执行；
11. 动态驱动自动调用服务；
12. 动态驱动自动写记忆；
13. 动态驱动自动生成 Skill；
14. 动态驱动自动合入；
15. 动态驱动覆盖架构宪法或安全底线。

────────────────────────────────
十、测试要求
────────────────────────────────

必须新增第六阶段相关测试。

A. 导入与兼容测试

1. L3 第六阶段对象可导入；
2. L3 第一至第五阶段对象仍可导入；
3. L0/L1/L2 未被修改；
4. 公共 __init__ 导出稳定；
5. 不导入 L4/L5/L6。

B. 观察回流测试

1. ObservationResultRef 可创建；
2. ObservationEnvelope 可创建；
3. ObservationFeedbackAdvice 可创建；
4. ObservationRoutingAdvice 可创建；
5. ObservationCredibilityScore 稳定；
6. ObservationConflictAdvice 可序列化；
7. ObservationResultRef 不采样真实观察；
8. ObservationFeedbackAdvice 不调用观察器。

C. 上下文续接测试

1. ContextCarryoverAdvice 可创建；
2. ContextWindowAdvice 可创建；
3. ContextCompressionNeedAdvice 可创建；
4. ContextRetentionAdvice 可创建；
5. ContextDropAdvice 可创建；
6. ContextValueScore 稳定；
7. ContextCarryoverAdvice 不写上下文存储；
8. ContextCompressionNeedAdvice 不调用模型摘要。

D. L6 服务请求编排测试

1. SubsystemServiceRequest 可创建；
2. MemoryServiceRequest 可创建；
3. RetrievalServiceRequest 可创建；
4. LearningServiceRequest 可创建；
5. AffectiveServiceRequest 可创建；
6. SubsystemServiceRouteRanking 稳定；
7. MemoryServiceRequest 不读取或写入记忆；
8. RetrievalServiceRequest 不执行检索；
9. LearningServiceRequest 不生成 Skill；
10. AffectiveServiceRequest 不计算情感；
11. 服务请求对象不调用插件宿主。

E. 候选提议流程测试

1. CandidateProposalAdvice 可创建；
2. CandidateSignalAdvice 可创建；
3. CandidateReviewRequestHint 可创建；
4. CandidateEvidenceRef 可创建；
5. CandidatePriorityScore 稳定；
6. CandidatePromotionAdvice 不自动合入；
7. CandidateRejectAdvice 不执行真实拒绝动作；
8. 候选提议不生成补丁、Skill、Tool。

F. 数学评分测试

1. ObservationCredibilityScore 稳定；
2. ContextValueScore 稳定；
3. MemoryNeedScore 稳定；
4. RetrievalNeedScore 稳定；
5. LearningSignalValueScore 稳定；
6. AffectiveNeedScore 稳定；
7. CandidatePriorityScore 稳定；
8. SubsystemServiceRecommendation 只输出 advice；
9. 评分不输出 execute / call_service / write_memory / retrieve / generate_skill；
10. 情感权重只改变排序倾向；
11. 动态驱动只改变排序倾向；
12. 服务请求准备度评分不等于服务执行授权。

G. 越界禁止项测试

必须断言生产代码中不存在真实越界行为：

1. 不调用 open 写文件；
2. 不调用 subprocess / os.system；
3. 不访问 requests / urllib / httpx；
4. 不调用模型客户端；
5. 不调用工具执行器；
6. 不实现 Observer / ObservationEngine；
7. 不读取桌面、文件、网络、日志、摄像头；
8. 不实现 MemoryStore / MemoryEngine；
9. 不实现 RetrievalEngine / SearchEngine；
10. 不实现 Embedding / RAG / VectorStore；
11. 不实现 LearningEngine；
12. 不实现 SkillGenerator / ToolGenerator；
13. 不实现 AffectiveEngine / EmotionEngine；
14. 不实现 PluginHost / SubsystemRouter Runtime；
15. 不实现 BoundaryDecision 真实裁决；
16. 不实现 ExecutionDispatcher Runtime；
17. 不恢复 AbilityPackage / CapabilityPort；
18. 不出现 shenshu / runtime 主链实现。

H. 序列化与快照测试

1. 所有新增公共对象稳定序列化；
2. hash 或等价 key 稳定；
3. 排序输出稳定；
4. reason_codes 稳定；
5. 无时间、随机数、uuid 漂移导致快照不稳定；
6. 服务请求对象、引用对象、候选建议可 diff。

────────────────────────────────
十一、开发步骤建议
────────────────────────────────

建议按以下顺序执行：

1. 解压并核对 L0/L1/L2/L3 第一至第五阶段前置材料；
2. 阅读 L3 总策划和第五阶段最终报告；
3. 确认 tiangong_kernel/l3_orchestration/ 现有目录结构；
4. 识别前五阶段基础对象、Run/Task/Turn/Step、Skill/ToolGroup、Intent、Boundary/Execution 请求对象；
5. 设计第六阶段观察回流、上下文续接、L6 服务请求、候选提议对象清单；
6. 先新增 ObservationResultRef 与 ObservationFeedbackAdvice；
7. 再新增 ContextCarryoverAdvice 与 ContextValueScore；
8. 再新增 SubsystemServiceRequest 基础对象；
9. 再新增 Memory / Retrieval / Learning / Affective 服务请求对象；
10. 再新增 CandidateProposalAdvice 与候选评分对象；
11. 新增观察/上下文/服务请求数学输入输出与评分对象；
12. 接入情感权重与动态驱动权重引用字段；
13. 接入 Run/Task/Turn/Step、Skill/ToolGroup、Intent、Boundary/Execution 上下文引用；
14. 完成稳定序列化；
15. 更新 __init__.py 公共导出；
16. 新增测试；
17. 运行第六阶段相关测试；
18. 运行 L3 相关兼容测试；
19. 运行 compileall；
20. 生成交付报告。

如发现 L3 第一至第五阶段存在阻断问题：
- 只允许做最小兼容修复；
- 必须在修改清单中单独列出；
- 不得借机重构；
- 不得改变阶段边界。

────────────────────────────────
十二、本阶段交付物要求
────────────────────────────────

完成后必须交付：

1. L3 第六阶段完整工程 zip；
2. L3 第六阶段开发日志；
3. L3 第六阶段 handoff 报告；
4. L3 第六阶段 validation 报告；
5. L3 第六阶段修改清单；
6. L3 第六阶段测试结果；
7. L3 第六阶段未做事项；
8. 如有兼容修复，提供兼容修复说明。

建议文件命名：

1. 天工造物_L3_第六阶段_观察回流上下文续接与L6服务请求编排_交付包_YYYYMMDD.zip
2. l3_phase6_development_log_zh.md
3. l3_phase6_handoff_report_zh.txt
4. l3_phase6_validation_report_zh.txt
5. l3_phase6_change_list_zh.txt
6. l3_phase6_test_results_zh.txt
7. l3_phase6_unfinished_items_zh.txt

报告必须明确：

1. 新增文件；
2. 修改文件；
3. 未修改 L0/L1/L2 的证据；
4. 未进入 L4/L5/L6 真实实现的证据；
5. 未调用模型/工具/网络/shell/存储的证据；
6. 未实现真实观察器、记忆、检索、学习、情感系统的证据；
7. 未生成真实 Skill / Tool / Knowledge 的证据；
8. 未执行真实服务请求的证据；
9. 数学评分只给建议的证据；
10. 情感与动态驱动只影响倾向的证据；
11. 测试命令和结果；
12. 未完成事项；
13. 是否建议进入第六阶段质检。

不得声称完整全量 pytest 通过，除非确实完整运行并记录命令、范围、耗时、结果。
如果只运行部分测试，必须明确写“只运行了哪些测试”。

────────────────────────────────
十三、第六阶段完成判定
────────────────────────────────

第六阶段完成必须同时满足：

1. ObservationResultRef 有纯引用对象；
2. ObservationFeedbackAdvice 有纯建议对象；
3. ContextCarryoverAdvice 有纯建议对象；
4. ContextValueScore 有评分对象；
5. SubsystemServiceRequest 有纯请求对象；
6. MemoryServiceRequest 有纯请求对象；
7. RetrievalServiceRequest 有纯请求对象；
8. LearningServiceRequest 有纯请求对象；
9. AffectiveServiceRequest 有纯请求对象；
10. CandidateProposalAdvice 有纯候选建议对象；
11. 观察可信度有评分对象；
12. 检索需要有评分对象；
13. 学习信号价值有评分对象；
14. 情感倾向权重有接入对象；
15. 服务请求路径排序有稳定输出；
16. 评分结果只作为建议；
17. 情感与动态驱动只作为权重输入；
18. 不实现真实观察器；
19. 不读写真实上下文存储；
20. 不读写真实记忆；
21. 不执行真实检索；
22. 不实现真实学习算法；
23. 不实现真实情感算法；
24. 不生成真实 Skill / Tool / Knowledge；
25. 不调用模型；
26. 不调用工具；
27. 不执行动作；
28. 不进入第七阶段至第八阶段；
29. 不进入 L4/L5/L6 真实实现；
30. 测试与报告齐全；
31. 交付包可解压、可导入、可运行相关测试。

若任一核心边界被破坏，本阶段不得判定完成。

────────────────────────────────
十四、禁止事项总清单
────────────────────────────────

本阶段严禁：

1. 修改 L0；
2. 修改 L1；
3. 修改 L2；
4. 开发第七阶段 Validation / Recovery / Iteration / Evolution；
5. 开发第八阶段总收口；
6. 进入 L4 真实执行层；
7. 进入 L5 真实边界层；
8. 进入 L6 真实子系统层；
9. 调用模型；
10. 调用工具；
11. 执行动作；
12. 执行 shell；
13. 读写外部文件；
14. 访问网络；
15. 使用数据库；
16. 实现插件宿主；
17. 实现观察系统；
18. 实现桌面观察；
19. 实现文件观察；
20. 实现网络观察；
21. 实现日志观察；
22. 实现记忆系统；
23. 读取真实记忆；
24. 写入真实记忆；
25. 实现检索系统；
26. 实现搜索系统；
27. 实现 embedding；
28. 实现 RAG；
29. 实现向量库；
30. 实现学习系统；
31. 生成真实 Skill；
32. 生成真实 Tool；
33. 生成真实知识；
34. 实现情感算法；
35. 实现情志计算；
36. 实现自我迭代；
37. 实现自我进化；
38. 实现权限裁决；
39. 实现风险放行；
40. 签发确认票据；
41. 授予租约；
42. 读取凭据；
43. 写审计；
44. 实现执行器；
45. 实现执行调度器；
46. 恢复旧能力包；
47. 恢复 CapabilityPort；
48. 恢复 AbilityPackagePort；
49. 恢复神枢或旧 Runtime 主链。

────────────────────────────────
十五、给工程员的开工口令
────────────────────────────────

开始 L3 第六阶段工程。

请先读取我上传的 L0 最终冻结归档包、L1 最终冻结归档包、L2 最终冻结归档包、L3 总策划交付包、L3 第一阶段最终工程包、L3 第二阶段最终工程包、L3 第三阶段最终工程包、L3 第四阶段最终工程包、L3 第五阶段最终工程包、L3 第一至第五阶段质检与修复报告，以及本 L3 第六阶段工程员提示词。

先确认前置材料是否齐全。
如果齐全，只开发 L3 第六阶段：观察回流、上下文续接、记忆/检索/学习/情感请求编排。

本阶段只做：
- ObservationResultRef / ObservationFeedbackAdvice / ObservationRoutingAdvice；
- ContextCarryoverAdvice / ContextWindowAdvice / ContextValueScore；
- SubsystemServiceRequest 纯请求对象；
- MemoryServiceRequest / RetrievalServiceRequest / LearningServiceRequest / AffectiveServiceRequest 纯请求对象；
- CandidateProposalAdvice / CandidateSignalAdvice / CandidateReviewRequestHint；
- 观察可信度评分；
- 上下文价值评分；
- 检索需要评分；
- 学习信号价值评分；
- 情感倾向权重接入；
- 服务请求路径排序；
- 状态转移建议；
- 与 Run/Task/Turn/Step、Skill/ToolGroup、Intent、Boundary/Execution 编排上下文的引用接线；
- 相关测试与报告。

本阶段不得：
- 不实现真实观察器；
- 不读写真实上下文存储；
- 不读写真实记忆；
- 不执行真实检索；
- 不实现 embedding / RAG / 向量库；
- 不实现真实学习算法；
- 不实现真实情感算法；
- 不生成真实 Skill / Tool / Knowledge；
- 不调用模型；
- 不调用工具；
- 不执行动作；
- 不做真实 L6 服务；
- 不进入第七阶段；
- 不进入 L4/L5/L6 真实实现。

完成后交付完整 zip、开发日志、handoff 报告、validation 报告、修改清单、测试结果、未做事项，并明确是否建议进入 L3 第六阶段专项质检。

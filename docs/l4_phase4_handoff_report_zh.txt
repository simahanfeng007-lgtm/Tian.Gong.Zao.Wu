天工造物 L4 第四阶段交接报告

阶段名称：第四阶段，模型动作、工具动作、ToolGroup 动作承载骨架
工程目录：project_l4_phase4
交接日期：2026-06-04

一、本阶段交付结论

第四阶段已完成，可作为第五阶段的输入基线。

本阶段没有进入第五阶段，没有实现真实文件、网络、终端、桌面动作，也没有启用真实模型或真实工具。

二、可继承接口

模型动作：

1. ModelActionRequest
2. ModelActionResult
3. ModelActionFailure
4. ModelActionFailureKind
5. ModelActionAdapterPort
6. FakeModelAdapter
7. DryRunModelAdapter

工具动作与 ToolGroup：

1. ToolArgumentEnvelope
2. ToolCallEnvelope
3. ToolResultEnvelope
4. ToolFailureEnvelope
5. ToolGroupActionContext
6. ToolActionRequest
7. ToolActionResult
8. ToolActionFailure
9. ToolActionFailureKind
10. ToolActionAdapterPort
11. FakeToolAdapter
12. DryRunToolAdapter

禁用与归一化：

1. DisabledRealModelToolAdapterStub
2. L3IntentBinding
3. ModelToolNormalization

三、边界状态

1. L0、L1、L2、L3 未修改。
2. L4 第一至第三阶段既有公共接口未被重构，第四阶段只追加 __init__.py 导出。
3. 未导入 L5 或 L6 真实实现。
4. 未恢复 Runtime、神枢、AbilityPackage、CapabilityPort、AbilityPackagePort 旧链路。
5. 假适配器和真实禁用占位分离。
6. 测试专用许可与生产许可隔离逻辑继承自第二阶段，回归测试通过。
7. 所有真实模型和真实工具路径默认禁用。

四、后续阶段建议

建议进入 L4 第五阶段。

第五阶段建议只围绕文件、网络、终端、桌面动作适配骨架与危险面隔离继续推进，并继续保持以下约束：

1. 不启用真实外部动作。
2. 不绕过 L3、L5、L6 分层。
3. 不写入 L2。
4. 不恢复旧能力包执行链。
5. 继续维持默认禁用、空跑优先、测试专用路径隔离。

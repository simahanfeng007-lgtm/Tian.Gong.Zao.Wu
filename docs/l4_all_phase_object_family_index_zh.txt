天工造物 L4 全阶段对象族索引

日期：2026-06-04

1. 基础执行对象族：ActionGroundingContext、ActionRequestIntake、ActionGroundingSession、ActionGroundingStep、ActionGroundingResult、ActionGroundingFailure、ActionGroundingSerialization、ActionGroundingProjection。
2. 许可门控引用对象族：ExecutionPermitRef、BoundaryDecisionRef、PolicyDecisionRef、ConfirmationTicketRef、LeaseRef、CredentialHandleRef、AuditRequirementRef、ResourceLimitRef、PermitScope、PermitExpiry、PermitValidationResult。
3. 适配器协议对象族：ActionAdapterProtocol、AdapterDescriptor、AdapterCapabilityDescriptor、AdapterRiskSurfaceDescriptor、AdapterRegistry、AdapterSelector、FakeAdapter、DryRunAdapter、NoOpAdapter、RealAdapterStub。
4. 模型/工具动作对象族：ModelActionRequest、ModelActionResult、ToolActionRequest、ToolActionResult、ToolGroupActionContext、ToolCallEnvelope、ToolArgumentEnvelope、ToolResultEnvelope、ToolFailureEnvelope。
5. 外部动作对象族：FileActionRequest、NetworkActionRequest、TerminalActionRequest、DesktopActionRequest、ExternalActionEnvelope、SideEffectDescriptor、ReversibilityDescriptor、ResourceUsageDescriptor、ExternalActionScope。
6. 结果/观察/失败对象族：ActionOutcomeEnvelope、ActionResultReturnEnvelope、ActionFailureReturnEnvelope、ExecutionObservationRef、ExecutionEvidenceRef、ExecutionAuditRef、ExecutionTraceRef、FailureCategory、FailureSeverity、FailureRecoverabilityHint。
7. 事务/资源/并发/回放对象族：ExecutionTransactionRef、ExecutionCommitIntent、ExecutionRollbackIntent、ResourceBudgetRef、ResourceUsageReport、ConcurrencyScope、ExecutionIsolationContext、ExecutionLockRef、ExecutionReplaySummary。
8. 总收口与交接对象族：L4ModuleInventory、L4PublicExportMap、L4ObjectFamilyIndex、L4BoundaryInvariantSuite、L4ToL5HandoffEnvelope、L4ToL6AdapterRequirement、L4FinalFreezeReadinessReport、L4FinalQualityChecklist。

9. L4→L5/L6 前置 8 专项补丁对象族：L4ActionAuditChain、SandboxPolicyRef、ExternalSurfaceActionRequest/Result/Failure、DisabledExternalSurfaceAdapterStub、DataGovernanceRefBundle、CommunicationEnvelopeBinding、HandoffActionBinding、L4VersionSwitchRequirement、L4ToL5PluginHostHandoffEnvelope、L4QualityGateHandoffEnvelope、L4TestEvidenceIndex、L4RegressionEvidenceIndex、L4ToL5AuditChainHandoff、L4ToL5VersionSwitchRequirement、L4ToL6MigrationSwitchRequirement。

说明：该索引只做对象族汇总，不重复定义已有对象，不创建运行时注册系统。

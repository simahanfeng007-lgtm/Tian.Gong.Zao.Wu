# 前置材料核对结论：五大模型高适配专项

## 已读取/识别文件

- 当前修复输入包：`天工造物_L5全阶段_hotfix2_情志专项修复包_20260605.zip`
- 当前专项提示词：`天工造物_五大模型高适配专项工程提示词_20260605.txt`
- 工程根：`project_l5_phase8_final_public_projection_l5_l6_handoff_freeze_hotfix2_affective_repair`
- 识别到 L0/L1/L2/L3/L4/L5 目录：`tiangong_kernel/l0_primitives`、`l1_ports`、`l2_state`、`l3_orchestration`、`l4_action_grounding`、`l5_plugin_host`
- 识别到 L5 Phase 8 / hotfix2 情志专项修复包命名与最终索引文件。

## 缺失文件

- 未在输入包中发现独立的 L4 第一至第八阶段完整工程包原始 zip。
- 未在输入包中发现独立的 L4 总收口报告、L4→L5/L6 交接说明原始文件。
- 本次按当前 L5 hotfix2 工程包内现有目录与文档进行兼容补丁，不回退前置阶段。

## 当前 L5 状态判断

- 当前 L5 处于：第八阶段总收口/公共投影/L5-L6 交接冻结后的 hotfix2 情志专项修复包。
- 当前 L5 主体不建议重构或覆盖。
- 本专项允许的修改范围：新增 L1-L4 模型治理链路兼容对象、L5 新增不变量/扫描辅助、测试和文档。
- 本专项不允许：把 L5 改成模型 API 适配层、让 L5 导入 provider SDK、让插件裸调模型。

## 链路现状

- L1 已有基础 ModelPort，但缺五 provider factsheet/descriptor/requirement/ref 完整链，本次已补 `model_provider_governance_ports.py`。
- L2 已有基础 `model_state.py`，但缺模型调用事实细分状态，本次已补 `model_interaction_state.py`。
- L3 已有 `model_intent.py`，但缺模型调用编排/治理检查/L4 dispatch 请求，本次已补 `model_invocation_flow.py`。
- L4 已有 adapter 体系，但缺五模型 provider adapter 协议、factsheet、mapper、disabled stub/live skeleton，本次已补 `model_provider_adapter.py`。
- L5 已有插件宿主治理基础，本次只新增 `model_capability_invariants.py`，不改 L5 主流程。
- L6 尚未开发，本次只输出前置禁止裸调规则。

## 是否可以进入本专项

可以进入。本次采用“最小兼容补丁 + 默认禁用真实调用 + L5 只增不破坏”的修复策略。

天工造物 L4→L5 交接说明

日期：2026-06-04

一、L5 接管事项

1. 权限裁决。
2. 风险评分。
3. 资源限额。
4. 确认票据。
5. 租约治理。
6. 凭据治理。
7. 审计门禁。
8. Permit 签发与验证。
9. BoundaryDecision 与 PolicyDecision。
10. ResourceBudget 与 ConcurrencyBudget。
11. RollbackPermission 与 ReplayPermission。
12. CredentialHandle 解析。
13. 拒绝、降级、要求确认路径。
14. L5 审计系统、边界复核、permit 生命周期和 scope 匹配策略。

二、L4 允许提交给 L5

1. L4ToL5HandoffEnvelope。
2. L4ToL5ExecutionAuditSummary。
3. L4ToL5BoundaryFeedback。
4. L4ToL5PermitConsumptionSummary。
5. L4ToL5ResourceBudgetSummary。
6. L4ToL5ConcurrencySummary。
7. audit_requirement_ref、boundary_feedback_ref、resource_usage_report、side_effect_summary、failure_category/severity/recoverability_hint。

三、L4 禁止提交给 L5

1. 明文凭据。
2. 明文敏感数据。
3. 真实状态写入结果。
4. 已自行放行的权限结论。
5. 已自行签发的 permit。
6. 已自行做出的 risk decision。
7. 已自行生成的 confirmation ticket。
8. 已自行延长的 lease。

四、结论

L4→L5 交接口径已冻结。L4 不实现 L5。

五、记忆与遗忘边界专项补丁说明

L5 后续需要对以下事项做边界治理：
1. 记忆写入 permit。
2. 记忆召回 permit。
3. 记忆注入 permit。
4. 遗忘治理 permit。
5. 删除与 Tombstone 票据。
6. 隐私、保留策略、同意和审计门禁。

L4 可提交：
1. ActionResultCognitiveSinkHint。
2. L4ToL6MemorySinkRequirement。
3. L4ToL6ForgettingSinkRequirement。
4. memory_candidate_refs、forgetting_intent_refs、retention_boundary_refs、learning_signal_refs、retrieval_followup_refs、privacy_review_refs。

L4 禁止：
1. 自行批准长期记忆写入。
2. 自行把召回记忆注入 Context。
3. 自行执行遗忘、删除、Tombstone 或匿名化。
4. 自行绕过隐私、审计、保留策略和用户同意边界。

六、自我学习 / 自我迭代 / 自我进化专项补丁说明

L5 后续需要治理：
1. CandidateValidatedToCommit 的提交门。
2. EvolutionCommitIntent 的许可与人工确认。
3. Activation / HotSwitch 的边界、租约和回滚锚点。
4. PostCommitObservation 的观察门禁。
5. RollbackAfterActivation 的验证门禁。
6. Tombstone / retirement / migration 的审计与兼容边界。

L4 可提交：
1. L4ToL5SelfEvolutionBoundaryRequirement。
2. L4ToL5SelfEvolutionPermitRequirement。

L4 禁止：
1. 自动合并候选。
2. 自动应用 patch。
3. 自动提交。
4. 自动热切换。
5. 自动回滚。
6. 将 L3 advice 当成 L5 permit。

七、自愈系统专项补丁说明

L5 后续需要治理：
1. RecoveryPermission。
2. RollbackPermission。
3. ReplayPermission。
4. CompensationPermission。
5. RecoveryLease。
6. AuditGate。
7. EvidencePolicy。
8. ConfirmationTicket。

L4 可提交：
1. L4SelfHealingHandoffRef。
2. L4FailureRecoveryRequirementBundle。
3. L4PostRecoveryValidationRequirement。

L4 禁止：
1. 自动 retry。
2. 自动恢复。
3. 自动 rollback。
4. 自动 replay。
5. 创建真实 checkpoint/snapshot。
6. 写 L2 状态或真实审计库。

八、Context / BeliefState / WorldState 安全边界专项补丁说明

L5 后续需要治理：
1. 工具结果进入上下文的许可。
2. 模型结果进入上下文的许可。
3. 记忆注入 Context 的过滤。
4. BeliefState 更新的事件事实优先级。
5. WorldState 可信度、过期性和证据边界。
6. 上下文污染、指令污染和 taint 边界。

L4 可提交：
1. L4ContextSafetyProjection。
2. L4ToolOutputContextProjection。
3. L4ModelOutputContextProjection。
4. L4ObservationBeliefWorldProjection。

L4 禁止：
1. 将工具输出升格为系统指令。
2. 直接把 normalized_output 拼入 Context。
3. 自行更新 BeliefState。
4. 自行同步 WorldState。
5. 绕过 L5 边界审查和 L6 Context Assembler。

九、L4→L5/L6 前置 8 专项补丁说明

L5 正式语义收口为插件宿主层。权限、风险、确认、租约、凭证、审计、资源预算等属于 BoundaryGate / SecurityBoundary / PermissionGate 横切能力，由 L5 插件宿主消费和治理，不再把 L5 简单等同为权限层。

L4 新增可交接对象：
1. L4ToL5PluginHostHandoffEnvelope。
2. L4ToL5QualityGateSummary。
3. L4ToL5AuditChainHandoff。
4. L4ToL5VersionSwitchRequirement。

L4ToL5HandoffEnvelope 已补充：
1. audit_chain_ref。
2. privacy_boundary_refs。
3. data_governance_refs。
4. external_disclosure_refs。
5. credential_token_refs。
6. token_revocation_refs。
7. credential_boundary_refs。
8. plugin_host_requirement_ref。
9. quality_gate_ref。
10. version_switch_requirement_ref。

L5 后续必须复核：审计证据责任链、插件 manifest/registry/lifecycle/isolation/health、测试验证回归质量门、热切换许可、数据治理同意与撤销、资源预算/配额/限流、HandoffGate 与外部消息降权。

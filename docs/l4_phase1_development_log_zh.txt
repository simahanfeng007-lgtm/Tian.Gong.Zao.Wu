天工造物 L4 第一阶段开发日志
生成日期：2026-06-04

一、阶段范围
本阶段只完成 L4 动作落地层第一阶段：动作落地基础骨架与安全默认拒绝骨架。L4 仅承载 L3 表达的动作请求；LLM 仍是连续执行主控者；L4 不选择 Skill、不推进流程、不做权限或风险裁决、不调用模型或工具、不执行真实文件/网络/终端/桌面动作。

二、前置材料核对结论
已读取：
1. L4 第一阶段交接包中的八阶段规划、工程规范、总工程师提示词、总策划交接说明、第一阶段工程员执行提示词。
2. L3 冻结候选包中的 l3_all_phase_closure_report_zh.txt、l3_final_freeze_candidate_after_p3_repair_zh.txt、P3 修复报告、测试结果、validation 报告。
3. L3 交接接口源码：execution_request.py 与 l3_to_l4_handoff.py。
4. L2 第九阶段修复后冻结候选材料：quality repair report、test results、validation report、final archive source index。

缺失或替代说明：
1. 未单独上传独立 L0 最终冻结 zip 与独立 L1 最终冻结 zip。
2. 本轮使用 L3 P3 修复后冻结候选包作为累计基座；其中已包含 L0/L1/L2/L3 累计源码、docs、tests 与 L2 第九阶段修复后冻结候选材料。
3. 当前工作区原 project 目录仅到 L2 较早阶段，未作为 L4 开发基座；本阶段基于 L3 冻结候选包创建 project_l4_phase1 工作副本。

基座判断：
1. L0 可作为原语基座：是。
2. L1 可作为端口协议基座：是。
3. L2 可作为状态基座：是，L2 第九阶段修复后完整 tests 502 passed。
4. L3 可作为编排基座：是，L3 总收口报告显示完整 tests 628 passed，L0/L1/L2 hash MATCH。
5. L3 P0/P1/P2：0；P3-001 已修复。
6. L3 -> L4 交接接口：存在，包含 ExecutionRequest、ExecutionDispatchRequest、ExecutionPlanRef、ExecutionStepRef 与 L3ToL4HandoffEnvelope。
7. 是否可以进入 L4 第一阶段：可以。

三、实现摘要
新增包：tiangong_kernel/l4_action_grounding/

新增对象：
1. ActionGroundingIdentity / ActionGroundingStatus。
2. ActionRequestIntake / ActionRequestIntakeSummary。
3. ActionGroundingContext / ActionGroundingSession / ActionGroundingStep。
4. ActionGroundingResult / ActionGroundingFailure / ActionGroundingError。
5. ActionGroundingInvariant / BoundaryPermitRequiredInvariant / NoLiveExecutionWithoutL5Invariant / NoL4AutonomousExecutionInvariant。
6. ExecutionDisabledByDefaultFailure。
7. ActionGroundingSerialization / ActionGroundingProjection。
8. FakeActionGroundingRunner / DryRunActionGroundingRunner / NoOpActionGroundingRunner。

四、边界处理
1. 公共对象使用 dataclass(frozen=True, slots=True)。
2. 序列化委托 L0 stable serialization，只使用标准库与既有 L0/L3 结构。
3. request intake 直接承载 L3 ExecutionRequest / ExecutionDispatchRequest / ExecutionPlanRef / ExecutionStepRef。
4. 无未来 L5 permit ref 时，runner 默认返回拒绝结果。
5. fake/dry-run/no-op 在 permit ref 存在时也只返回模拟、dry-run、no-op envelope，real_action_performed 始终为 False。
6. 未导入 L5/L6，未实现真实适配器，未写 L2 状态。

五、额外兼容修补
完整 tests 在 Windows 上暴露既有 L3 hash 守卫测试使用 str(Path) 导致路径分隔符不一致的问题。本阶段仅将 tests/test_l3_phase1_does_not_modify_l0_l1_l2.py 中 actual key 改为 path.relative_to(ROOT).as_posix()，保持与 baseline 的正斜杠路径一致。
该修补不修改 L0/L1/L2/L3 源码、不改变 L3 公共接口、不改变 hash 判断语义。

六、验证摘要
1. compileall：通过。
2. L4 第一阶段 targeted pytest：18 passed。
3. tests -k l4_phase1：18 passed, 628 deselected。
4. 完整 tests：646 passed。
5. L0/L1/L2/L3 Python 源码 hash 与 L3 冻结候选基座比对：MATCH。
6. L4 包越界关键词扫描：NO_MATCH。
7. L4 第一阶段禁止模块名扫描：NO_MATCH。

七、阶段结论
建议进入 L4 第二阶段规划/开发准备；第一阶段仅完成动作落地基础骨架与安全默认拒绝骨架，未启用任何真实外部动作。

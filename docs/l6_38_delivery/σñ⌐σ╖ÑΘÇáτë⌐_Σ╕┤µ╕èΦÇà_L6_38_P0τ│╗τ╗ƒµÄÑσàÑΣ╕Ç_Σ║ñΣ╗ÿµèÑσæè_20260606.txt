临渊者 L6.38【P0 系统接入一：Provider / Budget / Skill / Handoff】交付报告

一、结论
L6.38 已在 L6.37 执行链全线贯通冻结基线之上完成 Provider / Budget / Skill / Handoff 四个 P0 系统接入。接入点位于 tiangong_agent_runtime 与 tiangong_agent_shell 外壳层；tiangong_kernel 未发生变更。

二、接入结果
1. Provider：生成 ProviderProfile、ProviderExecutionTicket、CredentialRef；CredentialRef 只保存句柄，不保存、不读取、不展开凭证明文；无 L5 许可时 Provider smoke 降级为 dry-run / sample replay。
2. Budget：生成 StepBudgetLedger、ChainBudgetLease、TimeoutBudget、FailureBudget、BudgetSnapshot；只给预算账本、续租建议和降级建议，不直接扣费，不默认阻断 A0-A4。
3. Skill：生成 SkillCandidateRoute、SkillReviewTicket、SkillActivationIntent、SkillExecutionHint；候选和执行提示可被 Planner 消费，但不注册、不激活、不释放工具组。
4. Handoff：生成 SubtaskTicket、HandoffEnvelope、ParentChainCollectReport；仅表达可拆分子任务和父链回流结构，禁止自动递归派生。

三、主链保持
所有 L6.38 工具均以 RuntimeEntry 注册的 A2 工具进入 execute_plan，再由 PlannerExecutionController → LongChainRunner → ExecutionSpine 执行。不新增第二 Runtime，不新增裸工具执行通道。

四、边界结果
1. code_diff.changed_count=5，code_diff.added_count=2，removed_count=0。
2. tiangong_kernel hash compare：changed=0，added=0，removed=0。
3. forbidden scan blocking_findings_total=0。

五、验证摘要
1. compileall：通过。
2. L6.37 + L6.38：13 passed。
3. L6.31/L6.32/L6.34/L6.37/L6.38：55 passed。
4. Provider/Skill/L6.35/L6.36/L6.37/L6.38：42 passed。
5. 未声明完整全量 pytest 通过。

六、交付文件
完整包、增量包、交付报告、修改清单、验证结果、未做事项、integrity.json 均已生成。

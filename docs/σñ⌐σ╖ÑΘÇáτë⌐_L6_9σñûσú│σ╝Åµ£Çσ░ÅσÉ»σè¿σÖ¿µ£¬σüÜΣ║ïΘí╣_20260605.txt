天工造物 L6.9 外壳式最小智能体启动器未做事项
生成日期：2026-06-05

1. 未做真实 API 联调
- 需要用户提供真实 Provider、Base URL、API Key、model 后在本地验收。

2. 未做 python -m tiangong_kernel
- 本轮故意不新增 tiangong_kernel/__main__.py，避免污染 L6 主体。
- 若后续必须支持，只允许极薄转发到 tiangong_agent_shell，不得写业务逻辑。

3. 未做桌面端前端
- 当前是 CLI 启动器，不是 GUI。

4. 未做一键安装包
- 当前是源码级启动，不是 exe/dmg/AppImage/deb。

5. 未做三端安装验收
- 当前仅在当前 Linux 容器环境中进行 Python 层验证。

6. 未开放真实工具执行
- 工具桥默认 disabled。
- 后续必须接 RuntimeGovernedToolBridge 后才能进入真实工具执行链。

7. 未做长期记忆写入
- 当前 session 只保存在 CLI 进程内存中。

8. 未做多 Provider 深度差异适配
- 当前专用 Provider 先统一走 OpenAI-compatible 最小协议。
- DeepSeek/Qwen/GLM/MiniMax/MiMo/Claude/Gemini 差异适配留给 L7。
